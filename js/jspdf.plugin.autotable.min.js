(function(){
 if(!window.jspdf) return;
 const jsPDF = window.jspdf.jsPDF;
 jsPDF.prototype.autoTable = function(opts){
  // dummy table generator (Web2App safe)
  let y = opts.startY || 40;
  (opts.body||[]).forEach(row=>{
   this.text(row.join(" | "),14,y);
   y+=6;
  });
 };
})();